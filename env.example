# Configuración del Servidor
PORT=3000

# Base URL del servidor (se detecta automáticamente la IP del servidor)
# Si necesitas especificarla manualmente, descomenta y configura:
# BASE_URL=http://192.168.1.12:3000
# O puedes especificar solo la IP:
# SERVER_IP=192.168.1.12

# Configuración de Base de Datos
DB_HOST=localhost
DB_PORT=5432
POSTGRES_USER=atlas_user
POSTGRES_PASSWORD=atlas_password
POSTGRES_DB=atlas_metadata

# Configuración de Media
MEDIA_BASE_PATH=/usr/src/app/media

# Configuración de FFmpeg
FFMPEG_PATH=ffmpeg

# Health Check
HEALTH_CHECK_INTERVAL=30000

# Logs
MAX_LOG_ENTRIES_PER_CHANNEL=1000
LOG_LEVEL=INFO

# JWT (Autenticación)
# IMPORTANTE: JWT_SECRET se usa para firmar y verificar tokens de autenticación.
# En producción, DEBES usar un secreto seguro y aleatorio. 
# Para generar uno, ejecuta: openssl rand -base64 64 | tr -d '\n' | head -c 64
# NUNCA uses el valor por defecto en producción, es un riesgo de seguridad.
JWT_SECRET=atlas_secret_key_change_in_production
JWT_EXPIRES_IN=24h
