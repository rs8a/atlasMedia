const express = require('express');
const app = express();

app.get('/', (req, res) => res.send('Atlas Funcionando'));

app.listen(3000, '0.0.0.0', () => {
    console.log('Servidor Atlas listo en puerto 3000');
});
